<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.openlearning.scrumify">

    <application
        android:name=".AppClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Scrumify">
        <activity android:name=".ui.activities.TasksActivity"></activity>
        <activity android:name=".ui.activities.ProjectDetailsActivity" />
        <activity android:name=".ui.activities.ShowProjectsActivity" />
        <activity android:name=".ui.activities.UpdateUserInfoActivity" />
        <activity android:name=".ui.activities.HomeActivity" />
        <activity android:name=".ui.activities.AddNewProject" />
        <activity android:name=".ui.activities.AddNewTaskActivity" />
        <activity android:name=".ui.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.RegistrationActivity" />
        <activity android:name=".ui.activities.LoginActivity" />
        <activity android:name=".ui.activities.MainActivity" />
    </application>

</manifest>